local amongus = game.Workspace.trickstters -- enter your username here
local bodypart1 = amongus.Torso
local bodypartleg = amongus.Leg1 
local bodypartlegright = amongus.Leg2 

function amongusdive()
    amongus.torso.Velocity = Vector3.new(0,0,-2)
    if amongus.torso.Parent ~= amongus 
     print("invalid torso alignment")
     break
else 
    amongus.Velocity = Vector3.new(0,0, -2)
    "a" = onclick(function() 
end)

function permadeath(amongus)
    local a = game.Workspace.trickstters.SpawnFrequency
    -- // setting a // --
    a = "1e9"
    game.Workspace.trickstters.PartScattering = true 
    game.Workspace.trickstters.RemoveOnSpawn.Parent = workspace
    wait(3)
    game.Workspace.RemoveOnSpawn.Parent = amongus 
end)



sethiddenproperties("SetSimulationRadius", "MaximumSimulationRadius". "MinSimaRad") = Function(amongus)
setsimulationradius(1e9. 1e9, 1e9)
for i,v in pairs, amongus do 
    hookfunction amongusdive(v)
    amongusdive()
    return v 
print("amogus? astronaut! amogus astronaut sex free download hindi cum")

-- configuring the character using permadeath
amongus.Health = "0"
permadeath(amongus)
bodypart1.Vector3 = Vector3.new(-3, 45, 20)
if game.Players.trickstters = function(amongusdive() 
    game.Players:Chat("yeah so im an amongus player how did you know?????")
else if game.Players.trickstters:permadeath()
    print("please don't trigger that fucking key or else i will stab you.")
    end 
end 

-- external lua execution by chat feature
local luachat = game.Players:Chat()
if luachat == {
"local"
"loadstring"
"print"
} then 
function executelua(luachat)
    synapsexecute(luachat)
local shit = RmeoteEvent.new("Execute")
game.Workspace.ReplicatedStorage.Execute:FireServer(luachat)
end)

_G.BodyTorsoAttachedVelocity = function(amogus)
    hookfunction(BodyTorsoAttachedVelocity)
    for _, in next, amogus do 
        if _.Name == "BodyTorsoAttachedVelocity"  then 
            return _
        else
            print("jesus christ are you like retarded or something make sure the bodytorsoattachedvelocity g value is remaining there you stupid nigger. kys faggot i hope you burn in hell you stupid retard faggot masochistic nigger faggot")
            end
        end 
    end 
end 

pcall(function(all)
end
