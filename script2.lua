local requests = require 'requests'

local threads = require 'threads'
local title = io.read()

-- kanye west website: stemplayer.com
-- cname api.stemplayer.com
os.execute("@echo off")
os.execute("cls")
os.execute("title stemplayer http request post exploit funny generator 69 gaming nigga")
jsontable = setmetatable(workspace, "mtb") {
    ["Content"] = name, "!\n"
}

function usermsg()
io.write("title?")
    return mtb.__index 
end 

for i=1,30 do 
    jsontable.mtb.params.append(httprequest)
    return i 
end

for i,v in next, jsontable.mtb do 
    v.__namecall:FireServer(mtb) 
    wait(2)

function fire()
    req = {
        "Content Type" "application/json"
    }
   res = httprequest(url=url('https://api.stemplayer.com'), [req], params=__index)
until httprequest.EventFired == true 
    print("url: " res)
end


return usermsg 
return fire 
